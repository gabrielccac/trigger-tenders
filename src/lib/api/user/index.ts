/**
 * User-authenticated API endpoints (e.g. list user biddings, profile).
 * Uses a different token (e.g. Bearer) and endpoints than public/.
 * Add modules here when implementing user-specific calls.
 */

export {};
